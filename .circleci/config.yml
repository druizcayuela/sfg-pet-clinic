# Java Maven CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-java/ for more details
#
version: 2
jobs:
  build:
    docker:
      # specify the version you desire here
      - image: circleci/openjdk:11-browsers-legacy

    # Specify service dependencies here if necessary
    # CircleCI maintains a library of pre-built images
    # documented at https://circleci.com/docs/2.0/circleci-images/
    # - image: circleci/postgres:9.4

    working_directory: ~/repo

    environment:
      # Customize the JVM maximum heap limit
      MAVEN_OPTS: -Xmx3200m

    steps:
      - checkout
      - run:
          name: Analyze on SonarCloud Web
          command: mvn verify sonar:sonar -DskipTests -e -Dsonar.projectKey=druizcayuela_pet-clinic-web -Dsonar.organization=druizcayuela -Dsonar.host.url=https://sonarcloud.io
      - run:
          name: Analyze on SonarCloud Data
          command: mvn verify sonar:sonar -DskipTests -e -Dsonar.projectKey=druizcayuela_pet-clinic-data -Dsonar.organization=druizcayuela -Dsonar.host.url=https://sonarcloud.io


      # Download and cache dependencies
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "pom.xml" }}
            # fallback to using the latest cache if no exact match is found
            - v1-dependencies-

      - run: mvn install -DskipTests

      - run: mvn dependency:go-offline

      - save_cache:
          paths:
            - ~/.m2
          key: v1-dependencies-{{ checksum "pom.xml" }}

      # run tests!
      - run: mvn integration-test

workflows:
  version: 2
  build-test-and-deploy:
    jobs:
      - build
      - test1:
          requires:
            - build
          context: SonarCloud